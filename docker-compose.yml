networks:
  vlan10_dmz:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.10.0/24
  vlan20_servidores:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.20.0/24
  vlan40_gestion:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.40.0/24
  vlan50_seguridad:
    driver: bridge
    ipam:
      config:
        - subnet: 10.0.50.0/24

services:

# ── BALANCEADOR DE CARGA ─────────────────────────────────
  srv-lb:
    image: nginx:latest
    container_name: SRV-LB
    restart: always
    sysctls:
     - net.ipv4.icmp_echo_ignore_all=1
    ports:
      - "80:80"
    volumes:
      - ./nginx/lb.conf:/etc/nginx/nginx.conf
    networks:
      vlan10_dmz:
        ipv4_address: 10.0.10.5
    depends_on:
      - srv-web-01
      - srv-web-02

  # ── SERVIDOR WEB 1 ───────────────────────────────────────
  srv-web-01:
    image: nginx:latest
    container_name: SRV-WEB-01
    restart: always
    sysctls:
      - net.ipv4.icmp_echo_ignore_all=1
    volumes:
      - ./nginx/html:/usr/share/nginx/html
      - ./nginx/web.conf:/etc/nginx/nginx.conf
    networks:
      vlan10_dmz:
        ipv4_address: 10.0.10.10

  # ── SERVIDOR WEB 2 ───────────────────────────────────────
  srv-web-02:
    image: nginx:latest
    container_name: SRV-WEB-02
    restart: always
    sysctls:
      - net.ipv4.icmp_echo_ignore_all=1
    volumes:
      - ./nginx/html:/usr/share/nginx/html
      - ./nginx/web.conf:/etc/nginx/nginx.conf
    networks:
      vlan10_dmz:
        ipv4_address: 10.0.10.11

  # ── BASE DE DATOS — MySQL ────────────────────────────────
  srv-db-01:
    image: mysql:8.0
    container_name: SRV-DB-01
    restart: always
    sysctls:
      - net.ipv4.icmp_echo_ignore_all=1
    environment:
      MYSQL_ROOT_PASSWORD: Admin123
      MYSQL_DATABASE: transmilenio_db
      MYSQL_USER: tm_user
      MYSQL_PASSWORD: Admin123
    volumes:
      - ./mysql/init:/docker-entrypoint-initdb.d
      - db_data:/var/lib/mysql
    networks:
      vlan20_servidores:
        ipv4_address: 10.0.20.10

  # ── REPLICA MySQL ────────────────────────────────────────
  srv-db-02:
    image: mysql:8.0
    container_name: SRV-DB-02
    restart: always
    sysctls:
      - net.ipv4.icmp_echo_ignore_all=1
    environment:
      MYSQL_ROOT_PASSWORD: Admin123
      MYSQL_DATABASE: transmilenio_db
      MYSQL_USER: tm_user
      MYSQL_PASSWORD: Admin123
    volumes:
      - db_data_replica:/var/lib/mysql
    networks:
      vlan20_servidores:
        ipv4_address: 10.0.20.11

  # ── ARCHIVOS — Samba ─────────────────────────────────────
  srv-files-01:
    image: dperson/samba
    container_name: SRV-FILES-01
    restart: always
    sysctls:
      - net.ipv4.icmp_echo_ignore_all=1
    environment:
      - TZ=America/Bogota
    command: >
      -u "tm_admin;Admin123"
      -u "tm_operador;Oper123"
      -u "tm_conductor;Conductor123"
      -u "tm_gerente;Gerente123"
      -u "tm_cliente;Cliente123"
      -s "transmilenio;/samba/data;yes;no;no;tm_admin,tm_operador,tm_gerente"
      -s "publico;/samba/public;yes;no;yes;all"
      -s "conductores;/samba/conductores;yes;no;no;tm_conductor,tm_operador"
    volumes:
      - ./samba/data:/samba/data
      - samba_public:/samba/public
    ports:
      - "445:445"
      - "139:139"
    networks:
      vlan20_servidores:
        ipv4_address: 10.0.20.20

# ── PROMETHEUS ───────────────────────────────────────────
  prometheus:
    image: prom/prometheus:latest
    container_name: SRV-PROMETHEUS
    restart: always
    sysctls:
      - net.ipv4.icmp_echo_ignore_all=1
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    networks:
      vlan50_seguridad:
        ipv4_address: 10.0.50.10
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.enable-lifecycle'

  # ── GRAFANA ──────────────────────────────────────────────
  grafana:
    image: grafana/grafana:latest
    container_name: SRV-GRAFANA
    restart: always
    sysctls:
      - net.ipv4.icmp_echo_ignore_all=1
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=tm_admin
      - GF_SECURITY_ADMIN_PASSWORD=Admin123
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - grafana_data:/var/lib/grafana
    networks:
      vlan50_seguridad:
        ipv4_address: 10.0.50.11
    depends_on:
      - prometheus

  # ── SSH + NTP ────────────────────────────────────────────
  srv-ssh-ntp:
    build: ./ssh
    container_name: SRV-SSH-NTP
    restart: always
    sysctls:
      - net.ipv4.icmp_echo_ignore_all=1
    ports:
      - "2222:22"
      - "123:123/udp"
    networks:
      vlan40_gestion:
        ipv4_address: 10.0.40.10

  # ── NODE EXPORTER (métricas del sistema) ─────────────────
  node-exporter:
    image: prom/node-exporter:latest
    container_name: SRV-NODE-EXPORTER
    restart: always
    sysctls:
      - net.ipv4.icmp_echo_ignore_all=1
    ports:
      - "9100:9100"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    networks:
      vlan50_seguridad:
        ipv4_address: 10.0.50.12

volumes:
  db_data:
  db_data_replica:
  samba_public:
  prometheus_data:
  grafana_data:
